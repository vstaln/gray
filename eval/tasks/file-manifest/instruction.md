In the work directory there are several `.txt` files (possibly among other
files). Write `manifest.txt` with one line per `.txt` file:
`<basename> <byte-size>`, sorted by basename. Only top-level files, no recursion.
