Join `users.csv` (header `id,name`) with `orders.csv` (header `id,total`) on `id`.
Write `joined.csv` with header `id,name,total`, one row per order, sorted by `id`
numerically. Inner join: skip orders whose id has no user.
