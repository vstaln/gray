# local agent config (machine-specific, never commit)
/AGENTS.md

/target/
# local reference clones (never commit)
/reference/
/port/

# local gray state (fallback when $HOME unset)
.gray/

# dogfooder harness ships; runs stay local (seeded gray-homes inside
# runs/ contain real key copies, transcripts may quote secrets)
# ponytail: track the loop, never its output
/dogfood/runs/

# opencode / tooling
.commandcode/
.superpowers/
.sdd/

# secrets
.env
auth.json
gateway.yaml
pairing/

# build artifacts that should never be committed
dist/dl/

# rust / editor / os
**/*.rs.bk
*.pdb
.DS_Store
.idea/
.vscode/
*.swp
*~

# python bytecode
__pycache__/
*.pyc

# local website build + strays (never commit)
/site/
/fishing.html

# arxiv research corpus (never commit)
/papers/

# local scan / bench scratch (never commit)
# Why: 862b952 committed the Jev simplification scan into history — the
# API-key-gated scripts, ~1.17M tokens of raw judgments and the vendored
# TypeSafe skill. Nothing under crates/ reads them; they are working notes,
# not repo assets, so the paths are purged from history and blocked here.
/SIMPLIFY_*.md
/typesafe_*
/skills-lock.json
/. agents/

# sibling scratch (never stage, per
# docs/superpowers/plans/2026-09-18-questions-permissions-plugins.md)
/calculator.html
/graydemos/
/text_*.json
/readthisagent.md
/OVERNIGHT-PLAN.md
/HANDOFF-*.md

# arxiv research corpora stay local, like /papers/ above
/arxiv-mega/
