---
description: Say hello — minimal example skill proving gray's skill discovery works.
---

# hello-skill

The smallest skill gray loads: a directory containing a `SKILL.md`
whose frontmatter carries a `description`. The skill name defaults to
the directory name (`hello-skill`).

Run it with `/skills:hello-skill`. When loaded, reply with a friendly
one-line greeting.
